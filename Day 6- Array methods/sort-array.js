


var arr = ['a', 'e', 'i', 'o'];
console.log(arr);

// sort method is used to sort the elements in terms of their occurences

arr.sort();
console.log("Sorted array in terms of their occurences using sort()\t "+arr);
