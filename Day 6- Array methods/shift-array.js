

var arr = ['a', 'e', 'i', 'o'];
console.log(arr);

// shift methods is used to remove the first element from the array

arr.shift();
console.log("Deleted an elements from the first index position using shift() \t"+arr);
