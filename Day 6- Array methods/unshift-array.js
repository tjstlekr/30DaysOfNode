
var arr = ['a', 'e', 'i', 'o'];
console.log(arr);

// unshift methods is used to insert an element at the start of the array
arr.unshift('t');
console.log("Adding elements in the array at the begining of the array using unshift() \t "+arr);
