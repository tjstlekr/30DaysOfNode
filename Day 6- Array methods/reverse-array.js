
var arr = ['a', 'e', 'i', 'o'];
console.log(arr);


// Reverse method is used to revers the index position of the array elements such that the first elements becomes the last element and the last elements become the first element

arr.reverse();
console.log("Reversed index position of the array using reverse()\t "+arr);
